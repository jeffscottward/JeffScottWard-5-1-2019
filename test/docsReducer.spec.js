/* global describe, it */
import expect from 'expect.js'
import SampleStateJSON from '../SampleState.json'
import { docsReducer } from '../reducers/docsReducer'

describe('docsReducer functionality', () => {
  it('should handle SET_DOC', () => {
    // const newSetDocs = docsReducer(SampleStateJSON.docs, { action: { type: 'SET_DOC', payload: { }}})
    // expect(newSetDocs).to.be('____')
  })
  it('should handle REMOVE_DOC', () => {
    // const newRemovedDocs = docsReducer(SampleStateJSON.docs, { action: { type: 'REMOVE_DOC', payload: { }}})
    // expect(newRemovedDocs).to.be('____')
  })
  it('should handle SET_VISIBLE_DOCS', () => {
    // const newVisibleDocs = docsReducer(SampleStateJSON.docs, { action: { type: 'SET_VISIBLE_DOCS', payload: { }}})
    // expect(newVisibleDocs).to.be('____')
  })
})
